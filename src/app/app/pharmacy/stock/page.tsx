import { PackagePlus, Plus } from "lucide-react"; import { PageHeader } from "@/components/layout/page-header"; import { EmptyState } from "@/components/feedback/empty-state"; import { EntityDialog } from "@/components/forms/entity-dialog"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select"; import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table"; import { Badge } from "@/components/ui/badge"; import { requireModuleAccess } from "@/lib/auth/module-access"; import { hasPermission,PERMISSIONS } from "@/lib/auth/permissions"; import { listBatches,listMedicines,listStockMovements,listSuppliers } from "@/modules/pharmacy/service"; import { addBatch,addSupplier,changeBatchStatus,recordPatientReturnAction,recordStockAdjustmentAction,recordStockCountAction,recordSupplierReturnAction,recordWriteOffAction } from "../actions";
export default async function Page(){const t=await requireModuleAccess("pharmacy");const canReconcile=hasPermission(t,PERMISSIONS.PHARMACY_STOCK_MANAGE);const [b,m,s,movements]=await Promise.all([listBatches(t.organizationId),listMedicines(t.organizationId),listSuppliers(t.organizationId),listStockMovements(t.organizationId)]);return <div className="space-y-6"><div className="flex flex-wrap justify-between gap-2"><PageHeader title="Stock & Batches" description="FEFO-ready batch stock with supplier, lot, expiry, quarantine and recall status."/><div className="flex gap-2"><EntityDialog trigger={<Button variant="outline"><Plus/>Supplier</Button>} title="New supplier" action={addSupplier}><Label>Name<Input name="name" required/></Label><Label>Licence number<Input name="licenceNumber"/></Label><Label>Contact person<Input name="contactPerson"/></Label><Label>Phone<Input name="phone"/></Label><Label>Email<Input name="email" type="email"/></Label><Label>Address<Input name="address"/></Label></EntityDialog><EntityDialog trigger={<Button><PackagePlus/>Receive batch</Button>} title="Receive medicine batch" action={addBatch}><Label>Medicine<Select name="medicineId"><SelectTrigger><SelectValue placeholder="Select"/></SelectTrigger><SelectContent>{m.map(x=><SelectItem key={x.id} value={x.id}>{x.name}</SelectItem>)}</SelectContent></Select></Label><Label>Supplier<Select name="supplierId"><SelectTrigger><SelectValue placeholder="Optional"/></SelectTrigger><SelectContent>{s.map(x=><SelectItem key={x.id} value={x.id}>{x.name}</SelectItem>)}</SelectContent></Select></Label><Label>Batch number<Input name="batchNumber" required/></Label><Label>Barcode<Input name="barcode" placeholder="Scanned lot/GS1 code"/></Label><Label>Quantity<Input name="quantity" type="number" min="1" required/></Label><Label>Cost price<Input name="costPrice" type="number" step="0.01" required/></Label><Label>Manufactured<Input name="manufactureDate" type="date"/></Label><Label>Expiry<Input name="expiryDate" type="date" required/></Label><Label>Invoice reference<Input name="invoiceReference"/></Label></EntityDialog></div></div>{!b.length?<EmptyState icon={PackagePlus} title="No batches" description="Receive supplier stock to begin dispensing."/>:<Table><TableHeader><TableRow><TableHead>Medicine</TableHead><TableHead>Batch</TableHead><TableHead>Barcode</TableHead><TableHead>Supplier</TableHead><TableHead>Expires</TableHead><TableHead>Quantity</TableHead><TableHead>Status / action</TableHead></TableRow></TableHeader><TableBody>{b.map(x=><TableRow key={x.id}><TableCell>{x.medicine.name}</TableCell><TableCell>{x.batchNumber}</TableCell><TableCell className="text-xs text-muted-foreground">{x.barcode??"-"}</TableCell><TableCell>{x.supplier?.name??"-"}</TableCell><TableCell>{x.expiryDate.toLocaleDateString()}</TableCell><TableCell>{x.quantity}/{x.initialQuantity}</TableCell><TableCell><div className="space-y-2"><Badge variant={x.status==="AVAILABLE"?"outline":"destructive"}>{x.status}</Badge><form action={changeBatchStatus} className="flex flex-wrap gap-2"><input type="hidden" name="batchId" value={x.id}/><Select name="status" defaultValue={x.status==="DEPLETED"?"AVAILABLE":x.status}><SelectTrigger className="w-36"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="AVAILABLE">Available</SelectItem><SelectItem value="QUARANTINED">Quarantined</SelectItem><SelectItem value="RECALLED">Recalled</SelectItem></SelectContent></Select><Input name="reason" required placeholder="Reason" className="w-44"/><Button size="sm" variant="outline">Update</Button></form>{canReconcile&&<details className="text-xs"><summary className="cursor-pointer text-muted-foreground">Reconcile stock</summary><div className="mt-2 space-y-2"><form action={recordStockCountAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="batchId" value={x.id}/><Label className="text-xs">Physical count<Input name="countedQuantity" type="number" min="0" required className="h-8 w-20 text-xs"/></Label><Input name="reason" required placeholder="Reason" className="h-8 w-32 text-xs"/><Button size="sm" variant="outline" type="submit">Record count</Button></form><form action={recordStockAdjustmentAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="batchId" value={x.id}/><Label className="text-xs">Adjust by (+/-)<Input name="quantityDelta" type="number" required className="h-8 w-20 text-xs"/></Label><Input name="reason" required placeholder="Reason" className="h-8 w-32 text-xs"/><Button size="sm" variant="outline" type="submit">Adjust</Button></form><form action={recordWriteOffAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="batchId" value={x.id}/><Label className="text-xs">Write off<Input name="quantity" type="number" min="1" required className="h-8 w-20 text-xs"/></Label><Input name="reason" required placeholder="Reason" className="h-8 w-32 text-xs"/><Button size="sm" variant="outline" type="submit">Write off</Button></form><form action={recordSupplierReturnAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="batchId" value={x.id}/><Label className="text-xs">Return to supplier<Input name="quantity" type="number" min="1" required className="h-8 w-20 text-xs"/></Label><Input name="reason" required placeholder="Reason" className="h-8 w-32 text-xs"/><Input name="reference" placeholder="RMA ref" className="h-8 w-24 text-xs"/><Button size="sm" variant="outline" type="submit">Return</Button></form><form action={recordPatientReturnAction} className="flex flex-wrap items-end gap-2"><input type="hidden" name="batchId" value={x.id}/><Label className="text-xs">Patient return (record only)<Input name="quantity" type="number" min="1" required className="h-8 w-20 text-xs"/></Label><Input name="reason" required placeholder="Reason" className="h-8 w-32 text-xs"/><Button size="sm" variant="outline" type="submit">Record return</Button></form></div></details>}</div></TableCell></TableRow>)}</TableBody></Table>}{movements.length>0&&<div><h2 className="mb-2 text-sm font-medium">Recent stock movements</h2><Table><TableHeader><TableRow><TableHead>When</TableHead><TableHead>Medicine / batch</TableHead><TableHead>Type</TableHead><TableHead>Delta</TableHead><TableHead>Reason</TableHead></TableRow></TableHeader><TableBody>{movements.slice(0,50).map(x=><TableRow key={x.id}><TableCell className="text-xs">{x.recordedAt.toLocaleString()}</TableCell><TableCell className="text-xs">{x.batch.medicine.name} · {x.batch.batchNumber}</TableCell><TableCell><Badge variant="outline">{x.type.replaceAll("_"," ")}</Badge></TableCell><TableCell>{x.quantityDelta>0?`+${x.quantityDelta}`:x.quantityDelta}</TableCell><TableCell className="text-xs text-muted-foreground">{x.reason}</TableCell></TableRow>)}</TableBody></Table></div>}</div>}
