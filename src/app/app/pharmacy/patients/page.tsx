import { Users,Plus } from "lucide-react"; import { PageHeader } from "@/components/layout/page-header";import { EmptyState } from "@/components/feedback/empty-state";import { EntityDialog } from "@/components/forms/entity-dialog";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { requireModuleAccess } from "@/lib/auth/module-access";import { listPatients } from "@/modules/pharmacy/service";import { addPatient } from "../actions";
export default async function Page(){const t=await requireModuleAccess("pharmacy"),p=await listPatients(t.organizationId);return <div className="space-y-6"><div className="flex justify-between"><PageHeader title="Patients" description="Patient identity, contact, allergy and dispensing context."/><EntityDialog trigger={<Button><Plus/>New patient</Button>} title="New patient" action={addPatient}><Label>Patient number<Input name="patientNumber" required/></Label><Label>Full name<Input name="fullName" required/></Label><Label>Date of birth<Input name="dateOfBirth" type="date"/></Label><Label>Sex<Input name="sex"/></Label><Label>Phone<Input name="phone"/></Label><Label>Email<Input name="email" type="email"/></Label><Label>Address<Input name="address"/></Label><Label>Allergies<Input name="allergies"/></Label><Label>Notes<Input name="notes"/></Label></EntityDialog></div>{!p.length?<EmptyState icon={Users} title="No patients" description="Registered patients appear here."/>:<Table><TableHeader><TableRow><TableHead>Number</TableHead><TableHead>Name</TableHead><TableHead>Contact</TableHead><TableHead>Allergies</TableHead></TableRow></TableHeader><TableBody>{p.map(x=><TableRow key={x.id}><TableCell>{x.patientNumber}</TableCell><TableCell>{x.fullName}</TableCell><TableCell>{x.phone??x.email??"-"}</TableCell><TableCell>{x.allergies??"None recorded"}</TableCell></TableRow>)}</TableBody></Table>}</div>}
