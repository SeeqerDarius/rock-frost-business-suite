import { ClipboardList,Plus } from "lucide-react";import { PageHeader } from "@/components/layout/page-header";import { EmptyState } from "@/components/feedback/empty-state";import { EntityDialog } from "@/components/forms/entity-dialog";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { Badge } from "@/components/ui/badge";import { requireModuleAccess } from "@/lib/auth/module-access";import { listMedicines,listPatients,listPrescribers,listPrescriptions } from "@/modules/pharmacy/service";import { addPrescriber,addPrescription } from "../actions";
export default async function Page(){const t=await requireModuleAccess("pharmacy");const [rx,patients,docs,meds]=await Promise.all([listPrescriptions(t.organizationId),listPatients(t.organizationId),listPrescribers(t.organizationId),listMedicines(t.organizationId)]);return <div className="space-y-6"><div className="flex flex-wrap justify-between gap-2"><PageHeader title="Prescriptions" description="Validated prescribers, prescription lines, remaining quantities and expiry."/><div className="flex gap-2"><EntityDialog trigger={<Button variant="outline"><Plus/>Prescriber</Button>} title="Register prescriber" action={addPrescriber}><Label>Full name<Input name="fullName" required/></Label><Label>Registration number<Input name="registrationNumber" required/></Label><Label>Facility<Input name="facilityName"/></Label><Label>Phone<Input name="phone"/></Label></EntityDialog><EntityDialog trigger={<Button><Plus/>Prescription</Button>} title="New prescription" action={addPrescription}><Label>Prescription number<Input name="prescriptionNumber" required/></Label><Label>Patient<Select name="patientId"><SelectTrigger><SelectValue placeholder="Select patient"/></SelectTrigger><SelectContent>{patients.map(x=><SelectItem key={x.id} value={x.id}>{x.fullName}</SelectItem>)}</SelectContent></Select></Label><Label>Prescriber<Select name="prescriberId"><SelectTrigger><SelectValue placeholder="Select prescriber"/></SelectTrigger><SelectContent>{docs.map(x=><SelectItem key={x.id} value={x.id}>{x.fullName}</SelectItem>)}</SelectContent></Select></Label><Label>Prescribed date<Input name="prescribedAt" type="date" required/></Label><Label>Expires<Input name="expiresAt" type="date"/></Label><Label>Medicine<Select name="medicineId"><SelectTrigger><SelectValue placeholder="Select medicine"/></SelectTrigger><SelectContent>{meds.map(x=><SelectItem key={x.id} value={x.id}>{x.name}</SelectItem>)}</SelectContent></Select></Label><Label>Quantity<Input name="quantityPrescribed" type="number" min="1" required/></Label><Label>Dosage<Input name="dosage" required/></Label><Label>Frequency<Input name="frequency" required/></Label><Label>Duration<Input name="duration"/></Label><Label>Instructions<Input name="instructions"/></Label><Label>Clinical notes<Input name="clinicalNotes"/></Label></EntityDialog></div></div>{!rx.length?<EmptyState icon={ClipboardList} title="No prescriptions" description="Validated prescriptions appear here."/>:<Table><TableHeader><TableRow><TableHead>Number</TableHead><TableHead>Patient</TableHead><TableHead>Prescriber</TableHead><TableHead>Medicines</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{rx.map(x=><TableRow key={x.id}><TableCell>{x.prescriptionNumber}</TableCell><TableCell>{x.patient.fullName}</TableCell><TableCell>{x.prescriber.fullName}</TableCell><TableCell>{x.lines.map(l=><div key={l.id}>{l.medicine.name}: {l.quantityDispensed}/{l.quantityPrescribed}</div>)}</TableCell><TableCell><Badge variant="outline">{x.status}</Badge></TableCell></TableRow>)}</TableBody></Table>}</div>}
