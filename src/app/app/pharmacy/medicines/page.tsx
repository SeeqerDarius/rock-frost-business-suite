import { Pill, Plus } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header"; import { EmptyState } from "@/components/feedback/empty-state"; import { EntityDialog } from "@/components/forms/entity-dialog"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"; import { Switch } from "@/components/ui/switch"; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"; import { Badge } from "@/components/ui/badge"; import { requireModuleAccess } from "@/lib/auth/module-access"; import { hasPermission, PERMISSIONS } from "@/lib/auth/permissions"; import { listMedicines } from "@/modules/pharmacy/service"; import { addMedicine } from "../actions"; import { BarcodeLookup } from "./barcode-lookup";
export default async function Page(){const t=await requireModuleAccess("pharmacy"),items=await listMedicines(t.organizationId),can=hasPermission(t,PERMISSIONS.PHARMACY_MEDICINES_MANAGE);return <div className="space-y-6"><div className="flex justify-between"><PageHeader title="Medicines" description="Registered medicine catalogue and regulatory supply classification."/>{can?<EntityDialog trigger={<Button><Plus/>New medicine</Button>} title="New medicine" action={addMedicine}><div className="grid gap-3 sm:grid-cols-2"><Label>SKU<Input name="sku" required/></Label><Label>Name<Input name="name" required/></Label><Label>Generic name<Input name="genericName"/></Label><Label>Strength<Input name="strength"/></Label><Label>Dosage form<Input name="dosageForm"/></Label><Label>Unit<Input name="unit" defaultValue="unit" required/></Label><Label>Class<Select name="medicineClass" defaultValue="OTC"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{["OTC","PHARMACY_ONLY","PRESCRIPTION_ONLY","CONTROLLED"].map(x=><SelectItem key={x} value={x}>{x.replaceAll("_"," ")}</SelectItem>)}</SelectContent></Select></Label><Label>FDA registration<Input name="registrationNumber"/></Label><Label>Barcode<Input name="barcode"/></Label><Label>Selling price<Input name="sellingPrice" type="number" step="0.01" required/></Label><Label>Reorder point<Input name="reorderPoint" type="number" defaultValue="0" required/></Label></div><label className="flex gap-2"><Switch name="requiresPrescription"/>Requires prescription</label></EntityDialog>:null}</div><BarcodeLookup/>{!items.length?<EmptyState icon={Pill} title="No medicines" description="Add the licensed medicines this pharmacy supplies."/>:<Table><TableHeader><TableRow><TableHead>SKU</TableHead><TableHead>Medicine</TableHead><TableHead>Class</TableHead><TableHead>Price</TableHead><TableHead>Eligible stock</TableHead></TableRow></TableHeader><TableBody>{items.map(i=><TableRow key={i.id}><TableCell>{i.sku}</TableCell><TableCell><b>{i.name}</b><div className="text-xs text-muted-foreground">{[i.genericName,i.strength,i.dosageForm].filter(Boolean).join(" · ")}</div></TableCell><TableCell><Badge variant={i.medicineClass==="CONTROLLED"?"destructive":"outline"}>{i.medicineClass.replaceAll("_"," ")}</Badge></TableCell><TableCell>{i.sellingPrice.toFixed(2)}</TableCell><TableCell>{i.batches.filter(b=>b.status==="AVAILABLE"&&b.expiryDate>new Date()).reduce((s,b)=>s+b.quantity,0)} {i.unit}</TableCell></TableRow>)}</TableBody></Table>}</div>}
