import { ShoppingBag } from "lucide-react";import { PageHeader } from "@/components/layout/page-header";import { EmptyState } from "@/components/feedback/empty-state";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from "@/components/ui/table";import { Badge } from "@/components/ui/badge";import { requireModuleAccess } from "@/lib/auth/module-access";import { hasPermission,PERMISSIONS } from "@/lib/auth/permissions";import { listDispensings,listMedicines,listPatients,listPendingControlledDispenses,listPrescriptions } from "@/modules/pharmacy/service";import { approveControlledDispenseAction,completeDispensing,rejectControlledDispenseAction,reverseCompletedDispensing } from "../actions";
export default async function Page(){const t=await requireModuleAccess("pharmacy");const canApprove=hasPermission(t,PERMISSIONS.PHARMACY_RESTRICTED_APPROVE);const [sales,meds,patients,rx,pending]=await Promise.all([listDispensings(t.organizationId),listMedicines(t.organizationId),listPatients(t.organizationId),listPrescriptions(t.organizationId),listPendingControlledDispenses(t.organizationId)]);const open=rx.filter(x=>["ACTIVE","PARTIALLY_DISPENSED"].includes(x.status));return <div className="space-y-6"><PageHeader title="Dispensing" description="Prescription-aware FEFO dispensing that excludes expired, quarantined and recalled batches."/>{pending.length>0&&<Card><CardHeader><CardTitle>Pending controlled-drug approval</CardTitle></CardHeader><CardContent className="space-y-3">{pending.map(x=><div key={x.id} className="rounded-md border p-3 text-sm"><p>{x.dispensingNumber} · {x.patient?.fullName??"Walk-in"} · {x.total.toFixed(2)} · requested {x.dispensedAt.toLocaleString()}</p>{canApprove?<div className="mt-2 flex flex-wrap gap-2"><form action={approveControlledDispenseAction}><input type="hidden" name="dispensingId" value={x.id}/><Button size="sm" type="submit">Approve</Button></form><form action={rejectControlledDispenseAction} className="flex items-end gap-2"><input type="hidden" name="dispensingId" value={x.id}/><Input name="reason" required placeholder="Rejection reason" className="h-8 w-40 text-xs"/><Button size="sm" variant="outline" type="submit">Reject</Button></form></div>:<p className="mt-1 text-xs text-muted-foreground">Waiting for a second person to approve. The requester cannot approve their own request.</p>}</div>)}</CardContent></Card>}<Card><CardHeader><CardTitle>Complete dispensing</CardTitle></CardHeader><CardContent><form action={completeDispensing} className="grid gap-3 md:grid-cols-3"><Label>Dispensing number<Input name="dispensingNumber" required/></Label><Label>Patient<Select name="patientId"><SelectTrigger><SelectValue placeholder="Walk-in/optional"/></SelectTrigger><SelectContent>{patients.map(x=><SelectItem key={x.id} value={x.id}>{x.fullName}</SelectItem>)}</SelectContent></Select></Label><Label>Prescription<Select name="prescriptionId"><SelectTrigger><SelectValue placeholder="OTC/optional"/></SelectTrigger><SelectContent>{open.map(x=><SelectItem key={x.id} value={x.id}>{x.prescriptionNumber}</SelectItem>)}</SelectContent></Select></Label><Label>Medicine<Select name="medicineId"><SelectTrigger><SelectValue placeholder="Select"/></SelectTrigger><SelectContent>{meds.map(x=><SelectItem key={x.id} value={x.id}>{x.name}</SelectItem>)}</SelectContent></Select></Label><Label>Prescription line<Select name="prescriptionLineId"><SelectTrigger><SelectValue placeholder="OTC/optional"/></SelectTrigger><SelectContent>{open.flatMap(x=>x.lines).map(x=><SelectItem key={x.id} value={x.id}>{x.medicine.name} ({x.quantityPrescribed-x.quantityDispensed} remaining)</SelectItem>)}</SelectContent></Select></Label><Label>Quantity<Input name="quantity" type="number" min="1" required/></Label><Label>Discount<Input name="discount" type="number" step="0.01" defaultValue="0"/></Label><Label>Payment method<Input name="paymentMethod"/></Label><Label>Payment reference<Input name="paymentReference"/></Label><Button type="submit">Dispense</Button></form><p className="mt-2 text-xs text-muted-foreground">A dispense containing a controlled medicine goes to pending approval instead of completing immediately when the pharmacy maker-checker setting is on.</p></CardContent></Card>{!sales.length?<EmptyState icon={ShoppingBag} title="No dispensing records" description="Completed dispensing records appear here."/>:<Table><TableHeader><TableRow><TableHead>Number</TableHead><TableHead>Patient</TableHead><TableHead>Items</TableHead><TableHead>Total</TableHead><TableHead>Status / action</TableHead></TableRow></TableHeader><TableBody>{sales.map(x=><TableRow key={x.id}><TableCell>{x.dispensingNumber}<div className="text-xs text-muted-foreground">{x.dispensedAt.toLocaleString()}</div></TableCell><TableCell>{x.patient?.fullName??"Walk-in"}</TableCell><TableCell>{x.lines.map(l=><div key={l.id}>{l.medicine.name} · {l.batch.batchNumber} · {l.quantity}</div>)}</TableCell><TableCell>{x.total.toFixed(2)}</TableCell><TableCell><Badge variant={x.status==="COMPLETED"?"outline":"destructive"}>{x.status}</Badge>{x.status==="COMPLETED"&&<form action={reverseCompletedDispensing} className="mt-2 flex gap-2"><input type="hidden" name="dispensingId" value={x.id}/><Input name="reason" required placeholder="Reversal reason" className="w-44"/><Button size="sm" variant="destructive">Reverse</Button></form>}{x.reversalReason&&<p className="mt-1 text-xs text-muted-foreground">{x.reversalReason}</p>}{x.rejectionReason&&<p className="mt-1 text-xs text-muted-foreground">{x.rejectionReason}</p>}</TableCell></TableRow>)}</TableBody></Table>}</div>}
