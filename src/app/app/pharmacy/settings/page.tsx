import { PageHeader } from "@/components/layout/page-header";import { Button } from "@/components/ui/button";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Switch } from "@/components/ui/switch";import { requireModuleAccess } from "@/lib/auth/module-access";import { getPharmacySettings } from "@/modules/pharmacy/service";import { saveSettings } from "../actions";
export default async function Page(){const t=await requireModuleAccess("pharmacy"),s=await getPharmacySettings(t.organizationId);return <div className="space-y-6"><PageHeader title="Pharmacy Settings" description="Licence metadata, pharmacist responsibility, numbering and medicine-safety controls."/><Card><CardHeader><CardTitle>Operational controls</CardTitle></CardHeader><CardContent><form action={saveSettings} className="grid gap-4 md:grid-cols-2"><Label>Pharmacy licence number<Input name="licenceNumber" defaultValue={s.licenceNumber??""}/></Label><Label>Superintendent pharmacist<Input name="superintendentPharmacist" defaultValue={s.superintendentPharmacist??""}/></Label><Label>Registration number<Input name="superintendentRegistration" defaultValue={s.superintendentRegistration??""}/></Label><Label>Receipt prefix<Input name="receiptPrefix" defaultValue={s.receiptPrefix} required/></Label><Label>Prescription validity days<Input name="prescriptionValidityDays" type="number" min="1" defaultValue={s.prescriptionValidityDays}/></Label><Label>Expiry alert days<Input name="expiryAlertDays" type="number" min="1" defaultValue={s.expiryAlertDays}/></Label><label className="flex items-center gap-2"><Switch name="requirePatientForControlled" defaultChecked={s.requirePatientForControlled}/>Require patient for controlled medicines</label><Button type="submit">Save settings</Button></form></CardContent></Card><p className="text-sm text-muted-foreground">These settings support operations but do not validate licences or certify regulatory compliance.</p></div>}
