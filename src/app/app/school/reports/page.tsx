import { Lock } from "lucide-react";import { PageHeader } from "@/components/layout/page-header";import { EmptyState } from "@/components/feedback/empty-state";import { Card,CardHeader,CardTitle } from "@/components/ui/card";import { requireModuleAccess } from "@/lib/auth/module-access";import { hasPermission,PERMISSIONS } from "@/lib/auth/permissions";import { getSchoolSummary } from "@/modules/school/service";
export default async function SchoolReportsPage(){const t=await requireModuleAccess("school");if(!hasPermission(t,PERMISSIONS.SCHOOL_REPORTS_VIEW))return <EmptyState icon={Lock} title="School reports are restricted" description="Your role does not include school reporting."/>;const s=await getSchoolSummary(t.organizationId),stats=[["Active students",s.activeStudents],["Active classes",s.activeClasses],["Present records",s.attendance.PRESENT??0],["Absent records",s.attendance.ABSENT??0],["Fee collections",s.collections.toFixed(2)],["Outstanding fees",s.outstanding.toFixed(2)],["Overdue loans",s.overdueLoans],["Transport routes",s.activeRoutes]];return <div className="space-y-6"><PageHeader title="School Reports" description="Enrollment, attendance, collections, arrears, library, and transport indicators."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([l,v])=><Card key={l}><CardHeader><p className="text-xs text-muted-foreground">{l}</p><CardTitle className="text-2xl">{v}</CardTitle></CardHeader></Card>)}</div></div>}
