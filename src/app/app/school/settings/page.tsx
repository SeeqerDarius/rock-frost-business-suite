import { PageHeader } from "@/components/layout/page-header";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { requireModuleAccess } from "@/lib/auth/module-access";import { hasPermission,PERMISSIONS } from "@/lib/auth/permissions";import { listSchoolSettings } from "@/modules/school/service";import { upsertSchoolSettingsAction } from "../actions";
export default async function SchoolSettingsPage(){const t=await requireModuleAccess("school"),can=hasPermission(t,PERMISSIONS.SCHOOL_SETTINGS_MANAGE),campuses=await listSchoolSettings(t.organizationId);return <div className="space-y-6"><PageHeader title="School Settings" description="Attendance correction windows, receipt numbering, grading scales, and ranking policy."/>{campuses.map(c=><form key={c.id} action={upsertSchoolSettingsAction} className="grid gap-3 rounded-lg border p-4 lg:grid-cols-5"><input type="hidden" name="campusId" value={c.id}/><div className="font-medium lg:col-span-5">{c.name}</div><div><Label>Attendance close days</Label><Input name="attendanceCloseDays" type="number" defaultValue={c.settings?.attendanceCloseDays??7} disabled={!can}/></div><div><Label>Receipt prefix</Label><Input name="receiptPrefix" defaultValue={c.settings?.receiptPrefix??"SCH"} disabled={!can}/></div><div className="lg:col-span-2"><Label>Grading scale JSON</Label><Input name="gradingScaleText" defaultValue={c.settings?.gradingScale?JSON.stringify(c.settings.gradingScale):""} disabled={!can}/></div><label className="flex items-center gap-2 text-sm"><input name="allowRanking" type="checkbox" defaultChecked={c.settings?.allowRanking} disabled={!can}/>Allow rankings</label>{can?<Button size="sm">Save settings</Button>:null}</form>)}</div>}
