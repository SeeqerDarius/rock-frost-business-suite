import { Banknote } from "lucide-react";import { PageHeader } from "@/components/layout/page-header";import { EmptyState } from "@/components/feedback/empty-state";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { requireModuleAccess } from "@/lib/auth/module-access";import { hasPermission,PERMISSIONS } from "@/lib/auth/permissions";import { listSchoolPayrollAdjustments } from "@/modules/school/service";import { createPayrollAdjustmentAction } from "../actions";
export default async function SchoolPayrollPage(){const t=await requireModuleAccess("school"),can=hasPermission(t,PERMISSIONS.SCHOOL_PAYROLL_MANAGE),rows=await listSchoolPayrollAdjustments(t.organizationId);return <div className="space-y-6"><PageHeader title="School Payroll" description="Education-specific payroll inputs for workload, substitutes, and allowances. Final payroll remains owned by the Payroll module."/>{can?<form action={createPayrollAdjustmentAction} className="grid gap-2 rounded-lg border p-4 sm:grid-cols-2 lg:grid-cols-5"><Input name="employeeId" placeholder="HR employee ID" required/><Input name="period" placeholder="YYYY-MM" required/><Input name="type" placeholder="Allowance / substitute" required/><Input name="description" placeholder="Description" required/><Input name="amount" type="number" step="0.01" min="0.01" placeholder="Amount" required/><Button size="sm">Add payroll input</Button></form>:null}{rows.length===0?<EmptyState icon={Banknote} title="No school payroll inputs" description="Add education-specific inputs for later Payroll-module processing."/>:<div className="space-y-2">{rows.map(r=><div key={r.id} className="flex justify-between rounded-lg border p-3"><span>{r.period} · {r.description}</span><span className="text-sm text-muted-foreground">{r.type} · {r.amount.toFixed(2)} · {r.processedAt?"Processed":"Pending"}</span></div>)}</div>}</div>}
