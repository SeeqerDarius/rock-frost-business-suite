{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main Rock Frost window. Deliberately narrow: only the OS/process plugins the shell chrome needs, plus this app's own custom commands (db_*, credentials_*) registered in src/lib.rs: no filesystem, shell, or network-plugin access is granted, since every network call goes through the webview's own fetch() to the public API origin, not a Tauri plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "process:default"
  ]
}
